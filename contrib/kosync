# KOSync config

# Listen on IPv6 + IPv4 (Assuming /proc/sys/net/ipv6/bindv6only is at its default)
KOSYNC_ADDR="[::]:3000"

# Path to the TLS certs. MUST be set & valid, even w/ TLS disabled!
KOSYNC_CERT="tls/cert.pem"
KOSYNC_KEY="tls/key.pem"

# Disable TLS if set
#KOSYNC_NO_TLS="1"
